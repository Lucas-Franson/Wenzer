import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

describe("example test", () => {
    it('two is equals to one plus one', () => {
        expect(2).toEqual(1+1);
    })
})